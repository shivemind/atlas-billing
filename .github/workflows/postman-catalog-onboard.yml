name: Postman Catalog Onboard

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

permissions:
  contents: read

jobs:
  collection-run:
    name: Run API Collection
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Postman CLI
        run: curl -o- "https://dl-cli.pstmn.io/install/linux64.sh" | sh

      - name: Login to Postman
        run: postman login --with-api-key ${{ secrets.POSTMAN_API_KEY }}

      - name: Run Collection
        run: |
          postman collection run 52400725-fb3146c1-0198-413c-a140-6ca86658a193 \
            -e 52400725-55db4f35-9ac7-4f83-b06b-edccde48fc27 \
            --postman-api-key ${{ secrets.POSTMAN_API_KEY }}

      - name: Trigger Monitor Run
        run: |
          curl -sf -X POST "https://api.getpostman.com/monitors/52400725-1f11e404-e83b-4270-b30f-f43fa9f50b28/run" \
            -H "x-api-key: ${{ secrets.POSTMAN_API_KEY }}"

      - name: Summary
        if: always()
        run: |
          echo "### Postman Catalog Onboard - Atlas Billing" >> $GITHUB_STEP_SUMMARY
          echo "- **Collection**: 52400725-fb3146c1-0198-413c-a140-6ca86658a193" >> $GITHUB_STEP_SUMMARY
          echo "- **Monitor**: 52400725-1f11e404-e83b-4270-b30f-f43fa9f50b28" >> $GITHUB_STEP_SUMMARY
